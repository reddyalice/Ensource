channel 1 > 0 -> a
if e:
	if e:
		skip 1
		r = 3
		-> r
		
	else -> b
else if 1 > 1:
	-> a
else -> a

i = i[1]

i = transmute f32 * <- 1+1
i += 1

	i++
	a = sign i
	crystal a ^= 1
        @ Test thing
	a = ( a ? 2 : 3)
		spell : i32 sadas -> cast y -> a
	attach necr a
	golem crystal a : i8 = 3
	cast asd -> asd
	if t:
		if t:
			v : i32, d : f32 = 1,1.

i: i32* = -(-4) 
a = s{ a, 
      r:3 }